// Placeholder so tsc --noEmit succeeds before Task 6 adds real function files.
// The leading underscore excludes this file from Cloudflare Pages routing.
// Safe to delete once functions/api/_lib/validation.ts (or any other functions/**/*.ts) is added.
export {};
